package sc.small.chat.dbconnection;import java.sql.Connection;import java.sql.DriverManager;import java.sql.ResultSet;import java.sql.SQLException;import java.sql.Statement;import org.assertj.core.api.Assertions;import org.junit.jupiter.api.Test;class MySqlConnectionTest {    @Test    void showDatabaseVersion() {//        String jdbcUrl = "jdbc:mysql://db-n2566-kr.vpc-pub-cdb.ntruss.com:3306/mysql_db?serverTimezone=Asia/Seoul&characterEncoding=UTF-8";//        String username = "mysql_dba";//        String password = "gotka9080!";        String jdbcUrl = "jdbc:mariadb://db.internal.smallchat.kr:3306";        String username = "root";        String password = "F8u9PvZtrT";        try (Connection connection = DriverManager.getConnection(jdbcUrl, username, password)) {            System.out.println("Connected to the database!");            // 간단한 쿼리 실행 예시            try (Statement statement = connection.createStatement()) {                String query = "SELECT VERSION()";                try (ResultSet resultSet = statement.executeQuery(query)) {                    if (resultSet.next()) {                        String version = resultSet.getString(1);                        System.out.println("MySQL Database Version: " + version);//                        Assertions.assertThat(version).isEqualTo("8.0.34");                    }                }            }        } catch (SQLException e) {            System.out.println("Connection failed! Check stack trace for details.");            e.printStackTrace();        }    }}