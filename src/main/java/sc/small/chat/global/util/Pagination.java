package sc.small.chat.global.util;import org.springframework.data.domain.PageRequest;import sc.small.chat.global.dto.BasicRequestDTO;public class Pagination {    /**     * parameter 형     */    public static PageRequest of(int page, int size) {        int zeroBasePage = (page > 0) ? page - 1 : page;        return PageRequest.of(zeroBasePage, size); // zero-based page index    }    /**     * PageRequest 상속 구현체용     * Demo : demo/dto/UserFilterDTO     */    public static PageRequest of(BasicRequestDTO.PageRequest pageRequest) {        int zeroBasePage = (pageRequest.getPage() > 0) ? pageRequest.getPage() - 1 : pageRequest.getPage();        return PageRequest.of(zeroBasePage, pageRequest.getSize());    }}